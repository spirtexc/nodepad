# Cluster A

Part of a connected cluster. In the graph view, this node is linked to the hub and to Cluster B, so it appears close to them.

---

See [[graphview/example-hub|Hub file]] for the central node.

Also connected to [[graphview/example-cluster-b|Cluster B]].

The [[mermaid/example|Mermaid examples]] show diagram types.
